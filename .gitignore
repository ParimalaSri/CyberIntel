# Raw dataset (too large for git; re-downloaded via docs/DATA_SOURCE.md)
*.json
*.json.zst
!evals/**/*.json

# Pipeline output (regenerated by pipeline/ scripts, not source-controlled)
data/bronze/**
data/silver/**
data/gold/**
!data/**/.gitkeep

# Gold outputs the deployed app reads directly - small enough to ship, no secrets
!data/gold/accounts.parquet
!data/gold/contacts.jsonl

# Python
__pycache__/
*.pyc
.venv/
venv/

# Env / secrets
.env
*.local

# OS
.DS_Store
Thumbs.db

# Local server logs / sample scratch data
server_out.log
server_err.log
data/samples/
